# 小遥搜索 (XiaoyaoSearch) API 接口文档

## 目录
1. [接口概述](#1-接口概述)
2. [认证和错误处理](#2-认证和错误处理)
3. [数据模型](#3-数据模型)
4. [搜索服务API](#4-搜索服务api)
5. [插件化架构与数据源配置](#5-插件化架构与数据源配置v30新增) ⭐
6. [索引管理API](#6-索引管理api)
7. [AI模型配置API](#7-ai模型配置api)
8. [设置管理API](#8-设置管理api)
9. [系统管理API](#9-系统管理api)
10. [应用日志API](#10-应用日志api)
11. [实时消息接口](#11-实时消息接口)
12. [前端集成示例](#12-前端集成示例)
13. [错误码参考](#13-错误码参考)

## 1. 接口概述

### 基本信息
- **接口版本**: v3.0.0
- **基础URL**: `http://127.0.0.1:8000`
- **认证方式**: 本地应用，无需认证
- **数据格式**: JSON
- **字符编码**: UTF-8
- **插件支持**: 数据源插件（filesystem/yuque/feishu等）

### 交互式文档
- **Swagger UI**: `http://127.0.0.1:8000/docs`
- **ReDoc**: `http://127.0.0.1:8000/redoc`

### 技术栈
- **后端框架**: FastAPI + Uvicorn
- **数据验证**: Pydantic v2
- **异步支持**: asyncio
- **CORS**: 支持Electron跨域访问

## 2. 认证和错误处理

### 认证机制
```http
# 本地应用无需认证，支持Electron渲染进程访问
Origin: http://localhost:3000
```

### 统一错误响应格式
```json
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "请求参数验证失败",
    "details": "query字段不能为空"
  }
}
```

### 国际化错误响应 ✅
为了支持多语言前端，API 错误响应采用错误码设计，前端根据错误码映射到对应语言的翻译：

**错误码响应格式**：
```json
{
  "success": false,
  "error": {
    "code": "index_not_found",
    "message": "索引不存在"
  }
}
```

**前端翻译映射**：
- 后端返回标准化的错误码（如 `index_not_found`）
- 前端 HTTP 拦截器根据错误码查找对应语言的翻译
- 支持中英文双语切换，用户看到的是其选择语言的错误提示

**支持的错误码类型**：
- HTTP 状态码映射：`bad_request`, `unauthorized`, `forbidden`, `not_found`, `internalServerError` 等
- 业务错误码：`index_not_found`, `model_load_failed`, `file_too_large` 等
- 详细的错误码列表请参考 [第12节 - 错误码参考](#12-错误码参考)

### HTTP状态码规范
| 状态码 | 说明 | 使用场景 |
|--------|------|----------|
| 200 | 成功 | 请求处理成功 |
| 400 | 请求错误 | 参数验证失败、格式错误 |
| 404 | 资源不存在 | 索引任务、模型配置不存在 |
| 413 | 文件过大 | 上传文件超过50MB限制 |
| 500 | 服务器错误 | 内部异常、AI模型加载失败 |

## 3. 数据模型

### 枚举类型
```typescript
// 输入类型
enum InputType {
  TEXT = "text",
  VOICE = "voice",
  IMAGE = "image"
}

// 搜索类型
enum SearchType {
  SEMANTIC = "semantic",    // 语义搜索
  FULLTEXT = "fulltext",    // 全文搜索
  HYBRID = "hybrid"         // 混合搜索
}

// 文件类型
enum FileType {
  VIDEO = "video",
  AUDIO = "audio",
  DOCUMENT = "document",
  IMAGE = "image"
}
```

### 核心数据模型
```typescript
// 搜索请求
interface SearchRequest {
  query: string;                    // 搜索查询词 (1-500字符)
  input_type?: InputType;           // 输入类型，默认TEXT
  search_type?: SearchType;         // 搜索类型，默认HYBRID
  limit?: number;                   // 返回结果数量 (1-100)，默认20
  threshold?: number;               // 相似度阈值 (0.0-1.0)，默认0.7
  file_types?: FileType[];          // 文件类型过滤
}

// 多模态请求
interface MultimodalRequest {
  input_data: string;               // Base64编码的文件数据
  input_type: InputType;            // 输入类型voice或image
  duration?: number;                // 音频最长时长秒数 (1-120)，默认30
  limit?: number;                   // 返回结果数量 (1-100)，默认20
  threshold?: number;               // 相似度阈值 (0.0-1.0)，默认0.7
  file_types?: FileType[];          // 文件类型过滤
}

// 索引创建请求
interface IndexCreateRequest {
  folder_path: string;              // 索引文件夹路径
  file_types?: string[];            // 支持文件类型
  recursive?: boolean;              // 是否递归搜索子文件夹
}

// AI模型配置请求
interface AIModelConfigRequest {
  model_type: string;               // 模型类型 embedding/speech/vision/llm
  provider: string;                 // 提供商类型 local/cloud
  model_name: string;               // 模型名称 (1-100字符)
  config: Record<string, any>;      // 模型配置参数
}

// 搜索结果
interface SearchResult {
  file_id: number;
  file_name: string;
  file_path: string;
  file_type: FileType;
  relevance_score: number;          // 相关性分数 (0.0-1.0)
  preview_text: string;             // 预览文本 (最多200字符)
  highlight: string;                // 高亮片段
  created_at: string;               // 文件创建时间
  modified_at: string;              // 文件修改时间
  file_size: number;                // 文件大小(字节)
  match_type: string;               // 匹配类型 semantic/fulltext/hybrid
  video_frame?: VideoFrameResult;   // 视频画面搜索结果（v2.5新增）

  // 数据源字段（v3.0新增）
  source_type: string;              // 数据源类型（filesystem/yuque/feishu等）
  source_url?: string;              // 原文访问链接（可选）
  source_name: string;              // 数据源显示名称
}

// 视频画面搜索结果（v2.5新增）
interface VideoFrameResult {
  timestamp: number;                // 时间戳（秒）
  timestamp_formatted: string;      // 格式化时间戳 (HH:MM:SS)
  frame_preview: string;            // 预览图路径
  frame_width: number;              // 帧宽度（像素）
  frame_height: number;             // 帧高度（像素）
}

// 统一响应格式
interface SearchResponse {
  success: boolean;
  data: {
    results: SearchResult[];
    total: number;
    search_time: number;            // 搜索耗时(秒)
    query_used: string;             // 实际使用的查询词
    input_processed: boolean;       // 是否进行了输入预处理
  };
  message?: string;
}

interface MultimodalResponse {
  success: boolean;
  data: {
    converted_text: string;         // 转换后的文本
    confidence: number;             // 转换置信度
    search_results: SearchResult[];
    video_results?: SearchResult[]; // 视频画面搜索结果（v2.5新增，可选）
    file_info: {
      filename: string;
      size: number;
      content_type: string;
    };
    search_time?: number;           // 搜索耗时(秒)
    ai_models_used?: string[];      // 使用的AI模型列表
  };
  message?: string;
}

// 应用设置
interface AppSettings {
  app_name: string;
  version: string;
  search: {
    default_limit: number;
    default_threshold: number;
    max_file_size: number;
  };
  indexing: {
    max_concurrent_jobs: number;
    supported_formats: string[];
    auto_rebuild: boolean;
  };
  ui: {
    theme: string;
    language: string;
  };
}

// 设置项
interface SettingItem {
  id: number;
  setting_key: string;
  setting_value: string;
  setting_type: 'string' | 'integer' | 'boolean' | 'float' | 'json';
  description?: string;
  updated_at: string;
}

// 创建设置请求
interface CreateSettingRequest {
  key: string;
  value: any;
  type?: 'string' | 'integer' | 'boolean' | 'float' | 'json';
  description?: string;
}

// 更新设置请求
interface UpdateSettingRequest {
  value: any;
}

// 批量创建请求
interface BatchCreateRequest {
  settings: Array<{
    key: string;
    value: any;
    type?: 'string' | 'integer' | 'boolean' | 'float' | 'json';
    description?: string;
  }>;
}

// 重置设置请求
interface ResetRequest {
  default_settings: Array<{
    setting_key: string;
    setting_value: string;
    setting_type: string;
    description: string;
  }>;
}

// 应用日志
interface AppLog {
  timestamp: string;
  level: 'DEBUG' | 'INFO' | 'WARNING' | 'ERROR';
  logger: string;
  message: string;
  module: string;
  details?: Record<string, any>;
}

// 日志查询响应
interface LogsResponse {
  success: boolean;
  data: {
    logs: AppLog[];
    total: number;
    has_more: boolean;
  };
}

// 系统健康状态
interface SystemHealth {
  status: 'healthy' | 'degraded' | 'unhealthy';
  database: {
    status: string;
    response_time: number;
  };
  ai_models: Record<string, {
    status: string;
    memory_usage?: string;
    error?: string;
  }>;
  indexes: Record<string, {
    status: string;
    document_count: number;
  }>;
  timestamp: string;
}
```

## 4. 搜索服务API

### 4.1 文本搜索
```http
POST /api/search
Content-Type: application/json
```

**请求参数**
```json
{
  "query": "人工智能发展趋势",
  "input_type": "text",
  "search_type": "hybrid",
  "limit": 20,
  "threshold": 0.7,
  "file_types": ["document", "pdf"]
}
```

**响应示例**
```json
{
  "success": true,
  "data": {
    "results": [
      {
        "file_id": 1,
        "file_name": "AI发展报告.pdf",
        "file_path": "/Users/Downloads/AI发展报告.pdf",
        "file_type": "document",
        "relevance_score": 0.92,
        "preview_text": "人工智能技术在过去几年中取得了突破性进展...",
        "highlight": "人工智能技术在过去几年中取得了<em>突破性进展</em>",
        "created_at": "2024-01-15T10:30:00Z",
        "modified_at": "2024-01-15T10:30:00Z",
        "file_size": 2048576,
        "match_type": "hybrid",
        "source_type": "filesystem",
        "source_url": null,
        "source_name": "本地文件"
      },
      {
        "file_id": 101,
        "file_name": "API设计规范",
        "file_path": "https://www.yuque.com/docs/api-design",
        "file_type": "markdown",
        "relevance_score": 0.88,
        "preview_text": "本文档描述API设计规范...",
        "highlight": "本文档描述API<em>设计规范</em>",
        "created_at": "2024-01-10T09:00:00Z",
        "modified_at": "2024-01-15T14:20:00Z",
        "file_size": 102400,
        "match_type": "semantic",
        "source_type": "yuque",
        "source_url": "https://www.yuque.com/docs/api-design",
        "source_name": "语雀知识库"
      }
    ],
    "total": 2,
    "search_time": 0.15,
    "query_used": "人工智能发展趋势",
    "input_processed": false
  }
}
```

**数据源说明（v3.0新增）**
- `source_type`: 数据源类型标识
  - `filesystem`: 本地文件系统
  - `yuque`: 语雀知识库
  - `feishu`: 飞书知识库（未来支持）
- `source_url`: 原文访问链接，仅在线数据源提供
- `source_name`: 数据源显示名称，用于前端展示

### 4.2 多模态搜索
```http
POST /api/search/multimodal
Content-Type: multipart/form-data
```

**表单参数**
- `input_type`: 输入类型 (voice/image)
- `file`: 上传的文件 (最大50MB)
- `search_type`: 搜索类型 (semantic/fulltext/hybrid)，默认hybrid
- `limit`: 返回结果数量 (1-100)，默认20
- `threshold`: 相似度阈值 (0.0-1.0)，默认0.7
- `file_types`: 文件类型过滤（可多选）

**支持的文件格式**
- **语音**: mp3, wav, m4a, flac (最大120秒)
- **图像**: jpg, jpeg, png, webp (最大10MB)

**请求示例**
```http
POST /api/search/multimodal
Content-Type: multipart/form-data; boundary=----WebKitFormBoundary7MA4YWxkTrZu0gW

------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="input_type"
Content-Type: text/plain

voice
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="file"; filename="recording.wav"
Content-Type: audio/wav

[音频文件二进制数据]
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="search_type"
Content-Type: text/plain

hybrid
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="threshold"
Content-Type: text/plain

0.7
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="file_types"
Content-Type: text/plain

document
------WebKitFormBoundary7MA4YWxkTrZu0gW
Content-Disposition: form-data; name="file_types"
Content-Type: text/plain

audio
------WebKitFormBoundary7MA4YWxkTrZu0gW--
```

**响应示例**
```json
{
  "success": true,
  "data": {
    "converted_text": "请帮我找一些关于机器学习的资料",
    "confidence": 0.95,
    "search_results": [
      {
        "file_id": 2,
        "file_name": "机器学习入门.docx",
        "file_path": "/Documents/机器学习入门.docx",
        "file_type": "document",
        "relevance_score": 0.88,
        "preview_text": "机器学习是人工智能的一个重要分支...",
        "highlight": "机器学习是人工智能的一个重要分支",
        "created_at": "2024-01-10T09:20:00Z",
        "modified_at": "2024-01-10T09:20:00Z",
        "file_size": 1024576,
        "match_type": "semantic"
      }
    ],
    "file_info": {
      "filename": "recording.wav",
      "size": 1024000,
      "content_type": "audio/wav"
    }
  }
}
```

**视频画面搜索响应示例（v2.5新增）**
```json
{
  "success": true,
  "data": {
    "converted_text": "一个包含代码和讲解的教学视频画面",
    "confidence": 0.92,
    "search_results": [
      {
        "file_id": 456,
        "file_name": "Python教程.mp4",
        "file_path": "/Videos/Python教程.mp4",
        "file_type": "video",
        "relevance_score": 0.88,
        "preview_text": "Python编程语言教学视频...",
        "video_frame": {
          "timestamp": 150,
          "timestamp_formatted": "00:02:30",
          "frame_preview": "/api/frames/preview/456_150",
          "frame_width": 1920,
          "frame_height": 1080
        },
        "created_at": "2024-01-15T10:00:00Z",
        "modified_at": "2024-01-15T10:00:00Z",
        "file_size": 524288000,
        "match_type": "semantic"
      }
    ],
    "file_info": {
      "filename": "query_image.jpg",
      "size": 204800,
      "content_type": "image/jpeg"
    },
    "search_time": 0.35,
    "ai_models_used": ["cn-clip", "faiss"]
  }
}
```

### 4.3 搜索历史
```http
GET /api/search/history?limit=20&offset=0
```

**查询参数**
- `limit`: 返回数量 (1-100)
- `offset`: 偏移量

**响应示例**
```json
{
  "success": true,
  "data": {
    "history": [
      {
        "id": 1,
        "search_query": "人工智能发展趋势",
        "input_type": "text",
        "search_type": "hybrid",
        "ai_model_used": "bge-m3",
        "result_count": 5,
        "response_time": 0.15,
        "created_at": "2024-01-20T14:30:00Z"
      }
    ],
    "total": 1
  }
}
```

### 4.4 删除搜索历史
```http
DELETE /api/search/history/{id}
```

**路径参数**
- `id`: 搜索历史记录ID

**响应示例**
```json
{
  "success": true,
  "message": "搜索历史删除成功"
}
```

### 4.5 清空搜索历史
```http
DELETE /api/search/history
```

**响应示例**
```json
{
  "success": true,
  "message": "搜索历史已清空"
}
```

### 4.6 搜索建议
```http
GET /api/search/suggestions?query=人工智&limit=5
```

**查询参数**
- `query`: 搜索查询前缀
- `limit`: 返回建议数量 (1-20)，默认5

**响应示例**
```json
{
  "success": true,
  "data": {
    "suggestions": [
      "人工智能",
      "人工智能技术",
      "人工智能发展趋势",
      "人工智能应用",
      "人工智能算法"
    ],
    "query": "人工智",
    "total": 5
  }
}
```

### 4.7 视频帧预览（v2.5新增）
```http
GET /api/frames/preview/{file_id}_{timestamp}
```

**路径参数**
- `file_id`: 视频文件ID
- `timestamp`: 时间戳（秒）

**响应**
- Content-Type: `image/jpeg`
- 图片二进制数据

**使用场景**
- 用于在搜索结果中显示视频匹配帧的预览图
- 前端可直接使用 `<img>` 标签加载

**前端使用示例**
```html
<img :src="`/api/frames/preview/${result.file_id}_${result.video_frame.timestamp}`"
     :alt="result.file_name"
     style="max-width: 320px;" />
```

## 5. 插件化架构与数据源配置（v3.0新增）

### 5.1 插件配置方式说明

插件化架构采用**约定优于配置**的设计理念，无需通过API管理插件：

**插件管理方式**：
- 插件无需通过API管理
- 插件放到 `data/plugins/` 目录自动加载
- 配置通过 `config.yaml` 文件完成
- 系统启动时自动同步数据源

### 5.2 插件目录结构

```
data/plugins/
├── yuque/
│   ├── plugin.py           # 插件实现代码
│   ├── config.yaml         # 插件配置文件
│   └── requirements.txt    # 依赖列表（可选）
├── feishu/
│   ├── plugin.py
│   ├── config.yaml
│   └── requirements.txt
└── filesystem/
    ├── plugin.py
    └── config.yaml
```

### 5.3 配置文件格式

**语雀插件配置示例** (`data/plugins/yuque/config.yaml`)

```yaml
# 插件基本信息
plugin:
  id: yuque
  name: 语雀知识库
  version: "1.0.0"
  enabled: true

# 数据源配置
datasource:
  type: yuque
  api_token: "your_yuque_api_token_here"  # 语雀API Token
  repo_slug: "username/knowledge_base"    # 知识库路径
  base_url: "https://www.yuque.com/api/v2"

# 同步配置
sync:
  interval: 60          # 同步间隔（分钟），0表示手动同步
  batch_size: 50        # 每批处理文档数量
  timeout: 30           # 请求超时时间（秒）
```

### 5.4 配置项说明

| 配置项 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| plugin.id | string | 是 | 插件唯一标识 |
| plugin.name | string | 是 | 插件显示名称 |
| plugin.version | string | 是 | 版本号（semver格式） |
| plugin.enabled | boolean | 是 | 是否启用插件 |
| datasource.type | string | 是 | 数据源类型 |
| datasource.api_token | string | 是 | API认证令牌 |
| datasource.repo_slug | string | 是 | 知识库路径 |
| sync.interval | integer | 否 | 同步间隔（分钟），默认60 |
| sync.batch_size | integer | 否 | 批处理大小，默认50 |
| sync.timeout | integer | 否 | 请求超时（秒），默认30 |

### 5.5 配置文件加载流程

```
系统启动
    ↓
扫描 data/plugins/ 目录
    ↓
发现插件目录（yuque/、feishu/等）
    ↓
读取每个插件的 config.yaml
    ↓
验证配置有效性
    ↓
加载插件模块（plugin.py）
    ↓
初始化插件实例
    ↓
启动后台同步任务
```

### 5.6 插件管理操作

**启用插件**：
```bash
# 确保插件目录存在且配置文件中 enabled: true
# 系统启动时自动加载
```

**禁用插件**：
```bash
# 方法1：修改配置文件
# data/plugins/yuque/config.yaml
# plugin.enabled: false

# 方法2：重命名目录
mv data/plugins/yuque data/plugins/yuque.disabled
```

**新增插件**：
```bash
# 创建插件目录
mkdir -p data/plugins/my_datasource

# 添加插件代码、配置文件和依赖
# 重启系统自动加载
```

### 5.7 支持的数据源类型

| 数据源 | 类型标识 | 状态 | 说明 |
|--------|----------|------|------|
| 本地文件系统 | filesystem | ✅ 已实现 | 默认数据源 |
| 语雀知识库 | yuque | 🚧 规划中 | 在线文档协作平台 |
| 飞书知识库 | feishu | 🔮 未来支持 | 企业协作平台 |

---

## 6. 索引管理API

### 5.1 创建索引
```http
POST /api/index/create
Content-Type: application/json
```

**请求参数**
```json
{
  "folder_path": "/Users/Documents",
  "file_types": ["pdf", "txt", "md", "docx", "xlsx", "mp3", "mp4", "wav"],
  "recursive": true
}
```

**响应示例**
```json
{
  "success": true,
  "data": {
    "index_id": 1,
    "status": "processing",
    "message": "索引任务已创建并开始执行"
  }
}
```

### 5.2 查询索引状态
```http
GET /api/index/status/{index_id}
```

**路径参数**
- `index_id`: 索引任务ID

**响应示例**
```json
{
  "success": true,
  "data": {
    "index_id": 1,
    "folder_path": "/Users/Documents",
    "status": "processing",
    "progress": 45,
    "total_files": 100,
    "processed_files": 45,
    "error_count": 2,
    "started_at": "2024-01-20T10:00:00Z",
    "completed_at": null,
    "error_message": null
  }
}
```

### 5.3 删除索引
```http
DELETE /api/index/{index_id}
```

**响应示例**
```json
{
  "success": true,
  "message": "索引删除成功"
}
```

### 5.4 索引列表
```http
GET /api/index/list?status=completed&limit=10
```

**查询参数**
- `status`: 任务状态过滤 (pending/processing/completed/failed)
- `limit`: 返回数量

**响应示例**
```json
{
  "success": true,
  "data": {
    "indexes": [
      {
        "id": 1,
        "folder_path": "/Users/Documents",
        "status": "completed",
        "total_files": 100,
        "processed_files": 98,
        "error_count": 2,
        "created_at": "2024-01-20T10:00:00Z",
        "completed_at": "2024-01-20T10:15:00Z"
      }
    ],
    "total": 1
  }
}
```

## 7. AI模型配置API

### 6.1 更新AI模型配置
```http
POST /api/config/ai-model
Content-Type: application/json
```

**请求参数**
```json
{
  "model_type": "embedding",
  "provider": "local",
  "model_name": "bge-m3",
  "config": {
    "model_path": "/data/models/bge-m3",
    "device": "cpu",
    "embedding_dim": 1024,
    "max_length": 8192
  }
}
```

**响应示例**
```json
{
  "success": true,
  "message": "AI模型配置更新成功"
}
```

### 6.2 获取所有AI模型配置
```http
GET /api/config/ai-models
```

**响应示例**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "model_type": "embedding",
      "provider": "local",
      "model_name": "bge-m3",
      "config_json": "{\"model_path\": \"/models/bge-m3\", \"device\": \"cpu\"}",
      "is_active": true,
      "created_at": "2024-01-15T08:00:00Z",
      "updated_at": "2024-01-15T08:00:00Z"
    },
    {
      "id": 2,
      "model_type": "speech",
      "provider": "cloud",
      "model_name": "aliyun-asr",
      "config_json": "{\"api_key\": \"***\", \"endpoint\": \"https://nls-gateway.cn-shanghai.aliyuncs.com\"}",
      "is_active": false,
      "created_at": "2024-01-15T08:30:00Z",
      "updated_at": "2024-01-15T08:30:00Z"
    }
  ]
}
```

### 6.3 测试AI模型
```http
POST /api/config/ai-model/{model_id}/test
```

**路径参数**
- `model_id`: 模型配置ID

**响应示例**
```json
{
  "success": true,
  "data": {
    "model_id": 1,
    "test_passed": true,
    "response_time": 0.12,
    "test_message": "BGE-M3模型测试成功，响应时间：120ms"
  }
}
```

### 6.4 启用/禁用AI模型
```http
PUT /api/config/ai-model/{model_id}/toggle
```

**路径参数**
- `model_id`: 模型配置ID

**响应示例**
```json
{
  "success": true,
  "message": "AI模型状态切换成功",
  "data": {
    "model_id": 1,
    "is_active": false,
    "previous_status": true
  }
}
```

### 6.5 删除AI模型配置
```http
DELETE /api/config/ai-model/{model_id}
```

**路径参数**
- `model_id`: 模型配置ID

**响应示例**
```json
{
  "success": true,
  "message": "AI模型配置删除成功"
}
```

### 6.6 获取默认AI模型配置
```http
GET /api/config/ai-models/default
```

**响应示例**
```json
{
  "success": true,
  "data": [
    {
      "id": 1,
      "model_type": "embedding",
      "provider": "local",
      "model_name": "bge-m3",
      "is_default": true,
      "config_json": "{\"model_path\": \"/models/bge-m3\"}"
    },
    {
      "id": 4,
      "model_type": "speech",
      "provider": "local",
      "model_name": "faster-whisper",
      "is_default": true,
      "config_json": "{\"model_size\": \"base\"}"
    }
  ]
}
```

## 8. 设置管理API

### 7.1 获取所有设置项
```http
GET /api/settings/
```

**响应示例**
```json
[
  {
    "id": 1,
    "setting_key": "max_search_results",
    "setting_value": "50",
    "setting_type": "integer",
    "description": "最大搜索结果数",
    "updated_at": "2024-01-20T10:30:00Z"
  },
  {
    "id": 2,
    "setting_key": "theme",
    "setting_value": "dark",
    "setting_type": "string",
    "description": "界面主题",
    "updated_at": "2024-01-20T10:30:00Z"
  }
]
```

### 7.2 获取指定设置项
```http
GET /api/settings/{key}
```

**路径参数**
- `key`: 设置键名

**响应示例**
```json
{
  "id": 1,
  "setting_key": "max_search_results",
  "setting_value": "50",
  "setting_type": "integer",
  "description": "最大搜索结果数",
  "updated_at": "2024-01-20T10:30:00Z"
}
```

### 7.3 获取设置值（原始类型）
```http
GET /api/settings/{key}/value?default=20
```

**路径参数**
- `key`: 设置键名

**查询参数**
- `default`: 默认值（可选）

**响应示例**
```json
{
  "key": "max_search_results",
  "value": 50,
  "exists": true
}
```

### 7.4 创建设置项
```http
POST /api/settings/
Content-Type: application/json
```

**请求参数**
```json
{
  "key": "auto_save_interval",
  "value": 300,
  "type": "integer",
  "description": "自动保存间隔（秒）"
}
```

**响应示例**
```json
{
  "id": 3,
  "setting_key": "auto_save_interval",
  "setting_value": "300",
  "setting_type": "integer",
  "description": "自动保存间隔（秒）",
  "updated_at": "2024-01-20T11:00:00Z"
}
```

### 7.5 更新设置值
```http
PUT /api/settings/{key}
Content-Type: application/json
```

**路径参数**
- `key`: 设置键名

**请求参数**
```json
{
  "value": 100
}
```

**响应示例**
```json
{
  "id": 1,
  "setting_key": "max_search_results",
  "setting_value": "100",
  "setting_type": "integer",
  "description": "最大搜索结果数",
  "updated_at": "2024-01-20T11:15:00Z"
}
```

### 7.6 删除设置项
```http
DELETE /api/settings/{key}
```

**路径参数**
- `key`: 设置键名

**响应示例**
```json
{
  "success": true,
  "message": "设置项 max_search_results 删除成功"
}
```

### 7.7 批量创建设置项
```http
POST /api/settings/batch
Content-Type: application/json
```

**请求参数**
```json
{
  "settings": [
    {
      "key": "language",
      "value": "zh-CN",
      "type": "string",
      "description": "界面语言"
    },
    {
      "key": "enable_notifications",
      "value": true,
      "type": "boolean",
      "description": "启用通知"
    }
  ]
}
```

**响应示例**
```json
[
  {
    "id": 4,
    "setting_key": "language",
    "setting_value": "zh-CN",
    "setting_type": "string",
    "description": "界面语言",
    "updated_at": "2024-01-20T11:30:00Z"
  },
  {
    "id": 5,
    "setting_key": "enable_notifications",
    "setting_value": "true",
    "setting_type": "boolean",
    "description": "启用通知",
    "updated_at": "2024-01-20T11:30:00Z"
  }
]
```

### 7.8 重置为默认设置
```http
POST /api/settings/reset
Content-Type: application/json
```

**请求参数**
```json
{
  "default_settings": [
    {
      "setting_key": "max_search_results",
      "setting_value": "20",
      "setting_type": "integer",
      "description": "最大搜索结果数"
    },
    {
      "setting_key": "theme",
      "setting_value": "light",
      "setting_type": "string",
      "description": "界面主题"
    }
  ]
}
```

**响应示例**
```json
{
  "success": true,
  "message": "成功重置 2 个默认设置",
  "data": {
    "total_defaults": 2,
    "created_count": 2,
    "message": "成功重置 2 个默认设置"
  }
}
```

**支持的设置类型**
- `string`: 字符串类型
- `integer`: 整数类型
- `boolean`: 布尔类型（true/false）
- `float`: 浮点数类型
- `json`: JSON对象类型

## 9. 系统管理API

### 8.1 系统健康检查
```http
GET /api/system/health
```

**响应示例**
```json
{
  "success": true,
  "data": {
    "status": "healthy",
    "database": {
      "status": "connected",
      "response_time": 0.002
    },
    "ai_models": {
      "bge_m3": {
        "status": "loaded",
        "memory_usage": "2.1GB"
      },
      "faster_whisper": {
        "status": "not_loaded",
        "error": null
      }
    },
    "indexes": {
      "faiss_index": {
        "status": "ready",
        "document_count": 15420
      },
      "whoosh_index": {
        "status": "ready",
        "document_count": 15420
      }
    },
    "timestamp": "2024-01-20T15:30:00Z"
  }
}
```

### 8.2 系统运行状态
```http
GET /api/system/running-status
```

**响应示例**
```json
{
  "success": true,
  "data": {
    "index_status": "正常",
    "data_count": 15420,
    "today_searches": 89,
    "system_status": "正常",
    "last_update": "2024-01-20T15:30:00Z"
  }
}
```

**数据字段说明**
- `index_status`: 索引状态 (正常/异常/待索引)
- `data_count`: 索引文件数量
- `today_searches`: 今日搜索次数
- `system_status`: 系统状态
- `last_update`: 最后更新时间（最近索引任务完成时间）

## 10. 应用日志API

### 8.1 获取应用日志
```http
GET /api/system/logs?level=info&limit=100&offset=0
```

**查询参数**
- `level`: 日志级别过滤 (debug/info/warning/error)，默认info
- `limit`: 返回数量 (1-1000)，默认100
- `offset`: 偏移量，默认0

**响应示例**
```json
{
  "success": true,
  "data": {
    "logs": [
      {
        "timestamp": "2024-01-20T16:30:00Z",
        "level": "INFO",
        "logger": "app.search",
        "message": "搜索请求处理完成",
        "module": "search",
        "details": {
          "query": "人工智能",
          "results_count": 15,
          "processing_time": 0.23
        }
      },
      {
        "timestamp": "2024-01-20T16:29:45Z",
        "level": "WARNING",
        "logger": "app.index",
        "message": "文件解析失败",
        "module": "index",
        "details": {
          "file_path": "/docs/corrupted.pdf",
          "error": "PDF文件损坏"
        }
      }
    ],
    "total": 1247,
    "has_more": true
  }
}
```

### 8.2 下载日志文件
```http
GET /api/system/logs/download?date=2024-01-20
```

**查询参数**
- `date`: 日期 (YYYY-MM-DD格式)，可选，不提供则下载今日日志

**响应**
- Content-Type: `application/octet-stream`
- Content-Disposition: `attachment; filename="xiaoyao-search-2024-01-20.log"`

## 11. 实时消息接口（轮询方式）

### 9.1 索引进度查询
```javascript
// 轮询索引进度
async function pollIndexProgress(indexId) {
  try {
    const response = await fetch(`/api/realtime/index/${indexId}/progress`);
    const result = await response.json();

    if (result.success) {
      const { data } = result;
      console.log(`索引进度: ${data.progress}%`);
      console.log(`已处理: ${data.processed_files}/${data.total_files}`);

      // 如果任务完成，停止轮询
      if (data.is_completed) {
        clearInterval(pollingInterval);
        console.log('索引任务已完成');
      }
    }
  } catch (error) {
    console.error('获取进度失败:', error);
  }
}

// 每2秒轮询一次
const pollingInterval = setInterval(() => pollIndexProgress(1), 2000);
```

**请求参数:**
- `index_id` (路径参数): 索引任务ID

**响应格式:**
```json
{
  "success": true,
  "message": "获取索引进度成功",
  "data": {
    "index_id": 1,
    "status": "processing",
    "progress": 75,
    "processed_files": 75,
    "total_files": 100,
    "error_count": 0,
    "folder_path": "/path/to/folder",
    "started_at": "2024-01-20T10:00:00Z",
    "completed_at": null,
    "error_message": null,
    "is_completed": false,
    "timestamp": "2024-01-20T10:15:00Z"
  }
}
```

### 9.2 搜索建议查询
```javascript
// 获取搜索建议（带防抖）
let searchTimeout;
async function getSuggestions(query) {
  clearTimeout(searchTimeout);

  searchTimeout = setTimeout(async () => {
    try {
      const response = await fetch(`/api/realtime/search/suggestions?query=${encodeURIComponent(query)}&limit=5`);
      const result = await response.json();

      if (result.success) {
        const { suggestions } = result.data;
        console.log('搜索建议:', suggestions);
        // 更新UI显示建议
      }
    } catch (error) {
      console.error('获取建议失败:', error);
    }
  }, 300); // 300ms防抖延迟
}

// 输入框事件
document.getElementById('searchInput').addEventListener('input', (e) => {
  getSuggestions(e.target.value);
});
```

**请求参数:**
- `query` (查询参数): 搜索查询词
- `limit` (查询参数): 建议数量限制，默认5

**响应格式:**
```json
{
  "success": true,
  "message": "获取搜索建议成功",
  "data": {
    "query": "人工智",
    "suggestions": [
      "人工智能",
      "人工智能技术",
      "人工智能发展趋势",
      "人工智能应用",
      "���工智能算法"
    ],
    "total": 5,
    "timestamp": "2024-01-20T10:15:00Z"
  }
}
```

### 9.3 活跃任务查询
```javascript
// 获取所有活跃的索引任务
async function getActiveTasks() {
  try {
    const response = await fetch('/api/realtime/index/active-tasks');
    const result = await response.json();

    if (result.success) {
      const { active_tasks } = result.data;
      console.log(`当前有 ${active_tasks.length} 个活跃任务`);
      active_tasks.forEach(task => {
        console.log(`任务 ${task.id}: ${task.progress}%`);
      });
    }
  } catch (error) {
    console.error('获取活跃任务失败:', error);
  }
}
```

### 9.4 轮询配置建议
```javascript
// 获取推荐的轮询配置
async function getPollingConfig() {
  try {
    const response = await fetch('/api/realtime/polling-config');
    const result = await response.json();

    if (result.success) {
      const { intervals, recommendations } = result.data;
      console.log('推荐轮询间隔:', intervals);

      // 使用推荐间隔
      const indexProgressInterval = setInterval(() => {
        // 索引进度轮询
      }, intervals.index_progress);
    }
  } catch (error) {
    console.error('获取轮询配置失败:', error);
  }
}
```

**推荐轮询间隔:**
- 索引进度：2秒
- 搜索建议：300毫秒（配合防抖使用）
- 活跃任务：5秒

## 12. 前端集成示例

### 10.1 Axios配置（支持国际化错误处理）✅
```typescript
// api/config.ts
import axios from 'axios';
import { message } from 'ant-design-vue';
import i18n from '@/i18n/config';

// 获取翻译函数的辅助方法
const t = (key: string, params?: Record<string, any>): string => {
  return i18n.global.t(key, params);
};

const api = axios.create({
  baseURL: 'http://127.0.0.1:8000',
  timeout: 30000,
  headers: {
    'Content-Type': 'application/json'
  }
});

// 响应拦截器（支持国际化错误消息）
api.interceptors.response.use(
  response => response.data,
  error => {
    console.error(t('http.responseError'), error);

    if (error.response) {
      const { status, data } = error.response;

      switch (status) {
        case 400:
          message.error(data.detail || t('http.badRequest'));
          break;
        case 401:
          message.error(t('http.unauthorized'));
          break;
        case 403:
          message.error(t('http.forbidden'));
          break;
        case 404:
          message.error(t('http.notFound'));
          break;
        case 413:
          message.error(t('error.fileTooLarge'));
          break;
        case 500:
          message.error(t('http.internalServerError'));
          break;
        default:
          message.error(`${t('http.requestFailed')}: ${status}`);
      }
    } else if (error.request) {
      message.error(t('http.networkError'));
    } else {
      message.error(t('http.configError'));
    }

    return Promise.reject(error);
  }
);

export default api;
```

### 10.2 搜索服务封装
```typescript
// api/search.ts
import api from './config';

export interface SearchParams {
  query: string;
  input_type?: 'text' | 'voice' | 'image';
  search_type?: 'semantic' | 'fulltext' | 'hybrid';
  limit?: number;
  threshold?: number;
  file_types?: string[];
}

export class SearchService {
  // 文本搜索
  static async search(params: SearchParams) {
    return await api.post('/api/search', params);
  }

  // 多模态搜索
  static async multimodalSearch(
    inputType: 'voice' | 'image',
    file: File,
    searchType?: string,
    limit?: number,
    threshold?: number,
    fileTypes?: string[]
  ) {
    const formData = new FormData();
    formData.append('input_type', inputType);
    formData.append('file', file);

    if (searchType) {
      formData.append('search_type', searchType);
    }
    if (limit) {
      formData.append('limit', limit.toString());
    }
    if (threshold) {
      formData.append('threshold', threshold.toString());
    }
    if (fileTypes && fileTypes.length > 0) {
      fileTypes.forEach(fileType => {
        formData.append('file_types', fileType);
      });
    }

    return await api.post('/api/search/multimodal', formData, {
      headers: {
        'Content-Type': 'multipart/form-data'
      }
    });
  }

  // 搜索历史
  static async getHistory(limit = 20, offset = 0) {
    return await api.get(`/api/search/history?limit=${limit}&offset=${offset}`);
  }
}
```

### 10.3 索引管理封装
```typescript
// api/index.ts
import api from './config';

export class IndexService {
  // 创建索引
  static async createIndex(params: {
    folder_path: string;
    file_types?: string[];
    recursive?: boolean;
  }) {
    return await api.post('/api/index/create', params);
  }

  // 获取索引状态
  static async getIndexStatus(indexId: number) {
    return await api.get(`/api/index/status/${indexId}`);
  }

  // 删除索引
  static async deleteIndex(indexId: number) {
    return await api.delete(`/api/index/${indexId}`);
  }

  // 索引列表
  static async getIndexList(status?: string, limit = 10) {
    const params = new URLSearchParams();
    if (status) params.append('status', status);
    params.append('limit', limit.toString());

    return await api.get(`/api/index/list?${params.toString()}`);
  }
}
```

### 10.4 Vue组件示例
```vue
<!-- components/SearchInterface.vue -->
<template>
  <div class="search-interface">
    <!-- 搜索输入 -->
    <a-input-search
      v-model:value="searchQuery"
      placeholder="输入搜索内容..."
      size="large"
      @search="handleSearch"
      :loading="searching"
    />

    <!-- 多模态输入 -->
    <div class="multimodal-input">
      <a-upload
        :before-upload="handleMultimodalUpload"
        :show-upload-list="false"
        accept=".mp3,.wav,.jpg,.jpeg,.png"
      >
        <a-button type="primary" ghost>
          <CameraOutlined v-if="inputType === 'image'" />
          <AudioOutlined v-else />
          {{ inputType === 'image' ? '图片搜索' : '语音搜索' }}
        </a-button>
      </a-upload>
    </div>

    <!-- 搜索结果 -->
    <div class="search-results" v-if="results.length > 0">
      <a-list
        :data-source="results"
        item-layout="vertical"
        size="large"
      >
        <template #renderItem="{ item }">
          <a-list-item>
            <a-list-item-meta>
              <template #title>
                <a @click="openFile(item.file_path)">{{ item.file_name }}</a>
              </template>
              <template #description>
                <a-tag :color="getFileTypeColor(item.file_type)">
                  {{ item.file_type }}
                </a-tag>
                <!-- 视频画面搜索时间戳显示（v2.5新增） -->
                <a-tag v-if="item.file_type === 'video' && item.video_frame" color="purple">
                  <ClockCircleOutlined />
                  {{ item.video_frame.timestamp_formatted }}
                </a-tag>
                <span class="relevance-score">
                  相关性: {{ (item.relevance_score * 100).toFixed(1) }}%
                </span>
              </template>
            </a-list-item-meta>
            <!-- 视频画面搜索预览图（v2.5新增） -->
            <div v-if="item.video_frame" class="video-frame-preview">
              <img
                :src="`/api/frames/preview/${item.file_id}_${item.video_frame.timestamp}`"
                :alt="`${item.file_name} - ${item.video_frame.timestamp_formatted}`"
                style="max-width: 320px; border-radius: 8px;"
              />
              <div class="video-frame-info">
                <span>分辨率: {{ item.video_frame.frame_width }}x{{ item.video_frame.frame_height }}</span>
                <a-button size="small" @click="openVideoAtTimestamp(item.file_path, item.video_frame.timestamp)">
                  跳转播放
                </a-button>
              </div>
            </div>
            <div class="preview-text" v-else v-html="item.highlight"></div>
          </a-list-item>
        </template>
      </a-list>
    </div>
  </div>
</template>

<script lang="ts" setup>
import { ref, reactive } from 'vue';
import { message } from 'ant-design-vue';
import { SearchService } from '@/api/search';
import type { SearchResult } from '@/types/api';

const searchQuery = ref('');
const searching = ref(false);
const inputType = ref<'voice' | 'image'>('image');
const results = ref<SearchResult[]>([]);

const handleSearch = async () => {
  if (!searchQuery.value.trim()) {
    message.warning('请输入搜索内容');
    return;
  }

  searching.value = true;
  try {
    const response = await SearchService.search({
      query: searchQuery.value,
      search_type: 'hybrid',
      limit: 20
    });

    if (response.success) {
      results.value = response.data.results;
      message.success(`找到 ${response.data.total} 个相关文件`);
    }
  } catch (error) {
    message.error('搜索失败，请重试');
    console.error('Search error:', error);
  } finally {
    searching.value = false;
  }
};

const handleMultimodalUpload = async (file: File) => {
  const isImage = file.type.startsWith('image/');
  inputType.value = isImage ? 'image' : 'voice';

  try {
    const response = await SearchService.multimodalSearch(
      inputType.value,
      file,
      'hybrid',  // search_type
      20,       // limit
      0.7,      // threshold
      ['document', 'image'] // file_types 过滤
    );

    if (response.success) {
      results.value = response.data.search_results;
      message.success(`语音识别: "${response.data.converted_text}"`);
    }
  } catch (error) {
    message.error('多模态搜索失败');
    console.error('Multimodal search error:', error);
  }

  return false; // 阻止默认上传行为
};

const openFile = (filePath: string) => {
  // 打开文件的逻辑
  window.api.openFile(filePath);
};

const getFileTypeColor = (type: string) => {
  const colors = {
    document: 'blue',
    image: 'green',
    audio: 'orange',
    video: 'red'
  };
  return colors[type] || 'default';
};
</script>
```

## 13. 错误码参考

### 11.1 通用错误码
| 错误码 | HTTP状态码 | 说明 |
|--------|------------|------|
| SUCCESS | 200 | 操作成功 |
| VALIDATION_ERROR | 400 | 请求参数验证失败 |
| UNAUTHORIZED | 401 | 未授权访问 |
| FORBIDDEN | 403 | 禁止访问 |
| NOT_FOUND | 404 | 资源不存在 |
| METHOD_NOT_ALLOWED | 405 | 请求方法不允许 |
| PAYLOAD_TOO_LARGE | 413 | 请求体过大 |
| UNSUPPORTED_MEDIA_TYPE | 415 | 不支持的媒体类型 |
| RATE_LIMIT_EXCEEDED | 429 | 请求频率超限 |
| INTERNAL_ERROR | 500 | 服务器内部错误 |
| SERVICE_UNAVAILABLE | 503 | 服务不可用 |

### 11.2 业务错误码
| 错误码 | 说明 | 解决方案 |
|--------|------|----------|
| SEARCH_QUERY_EMPTY | 搜索查询不能为空 | 检查query���数 |
| SEARCH_THRESHOLD_INVALID | 相似度阈值超出范围 | 使用0.0-1.0之间���值 |
| FILE_TOO_LARGE | 文件大小超过限制 | 压缩文件或分割上传 |
| UNSUPPORTED_FILE_TYPE | 不支持的文件类型 | 使用支持的文件格式 |
| FOLDER_NOT_FOUND | 文件夹不存在 | 检查folder_path参数 |
| INDEX_JOB_RUNNING | 索引任务正在运行 | 等待当前任务完成或取消 |
| MODEL_NOT_FOUND | AI模型配置不存在 | 检查model_id或创建新配置 |
| MODEL_LOAD_FAILED | AI模型加载失败 | 检查模型路径和依赖环境 |
| DATABASE_CONNECTION_FAILED | 数据库连接失败 | 检查数据库文件权限 |
| INSUFFICIENT_MEMORY | 内存不足 | 释放内存或增加系统内存 |

### 11.3 错误处理最佳实践
```typescript
// 统一错误处理函数
const handleApiError = (error: any) => {
  if (error.response) {
    const { status, data } = error.response;

    switch (status) {
      case 400:
        message.error(data.error?.details || '请求参数错误');
        break;
      case 404:
        message.error('请求的资源不存在');
        break;
      case 413:
        message.error('文件大小超过限制（50MB）');
        break;
      case 500:
        message.error('服务器内部错误，请稍后重试');
        break;
      default:
        message.error(`请求失败 (${status})`);
    }
  } else if (error.request) {
    message.error('网络连接失败，请检查后端服务');
  } else {
    message.error('请求配置错误');
  }
};

// 使用示例
try {
  const response = await SearchService.search(params);
  // 处理成功响应
} catch (error) {
  handleApiError(error);
}
```

---


## 模型选型



## 更新日志

### v3.0.0 (2026-02-22)
- **插件化架构与语雀数据源** 🚧 规划中
  - **核心价值**：
    - 扩展产品边界，从本地搜索升级为全场景知识搜索
    - 提升用户粘性，云端数据源绑定增强平台依赖
    - 建立插件生态，降低后续数据源接入成本
  - **技术实现**：
    - Python ABC定义DataSourcePlugin抽象基类
    - importlib实现插件热插拔
    - 配置文件管理（data/plugins/*/config.yaml）
    - 系统启动时自动发现和加载插件
  - **数据模型更新**：
    - ✅ `SearchResult` 扩展：新增 `source_type`、`source_url`、`source_name` 字段
  - **新增章节**：
    - ✅ 第5节：插件化架构与数据源配置
  - **配置管理**：
    - 后端 .env 配置：PLUGIN_DIR、PLUGIN_AUTO_DISCOVER
    - 插件配置文件：data/plugins/yuque/config.yaml
  - **支持的数据源**：
    - ✅ filesystem（本地文件系统）
    - 🚧 yuque（语雀知识库 - 规划中）
    - 🔮 feishu（飞书知识库 - 未来支持）

### v2.5.0 (2026-02-05)
- **视频画面搜索功能** ⏸️ 已暂停
  - **暂停原因**：优先开发插件化架构与语雀数据源特性
  - **技术方案**：FFmpeg + CN-CLIP（性能优2倍，无需临时文件）
  - **核心功能**（规划中）：
    - 视频关键帧提取（可配置间隔和时长）
    - 图搜图扩展支持视频帧搜索
    - 搜索结果包含时间戳和预览图
  - **数据模型更新**：
    - ✅ `VideoFrameResult` 接口：时间戳、格式化时间、预览图路径
    - ✅ `SearchResult` 扩展：可选 `video_frame` 字段
    - ✅ `MultimodalResponse` 扩展：可选 `video_results` 字段
  - **新增API**：
    - ✅ GET /api/frames/preview/{file_id}_{timestamp} - 视频帧预览图
  - **接口文档更新**：
    - 第3节：新增视频画面搜索数据模型
    - 第4节：新增视频画面搜索响应示例
    - 第10节：更新前端集成示例（Vue组件视频帧渲染）
  - **配置管理**：
    - 后端 .env 配置：VIDEO_FRAME_SEARCH_ENABLED, VIDEO_FRAME_INTERVAL, VIDEO_FRAME_MAX_DURATION

### v1.3.0 (2025-12-29)
- **国际化支持（i18n）** ✅ 已完成
  - **前端国际化**：vue-i18n@9 + Ant Design Vue locale + Day.js
    - 720+ 翻译键，覆盖所有页面和组件
    - 支持中英文双语无缝切换（zh-CN, en-US）
    - LocalStorage 语言偏好持久化
    - 语言切换器 UI 组件
    - 工厂函数模式确保响应式翻译更新
  - **后端国际化支持**：API 错误码设计
    - HTTP 错误码标准化，支持前端翻译
    - 统一错误响应格式
    - 错误码映射表
  - **前端集成示例更新**：
    - Axios 拦截器国际化错误处理
    - 完整的错误码翻译映射示例
- **接口文档更新**:
  - 第2节：国际化错误响应说明
  - 第10节：支持国际化的前端集成示例
  - 完善错误码参考和翻译映射

### v1.2.1 (2025-12-15)
- **多模态搜索增强**: 新增文件类型过滤支持
  - ✅ POST /api/search/multimodal - 新增 `file_types` 参数支持
  - ✅ 新增 `search_type`, `limit`, `threshold` 参数支持
  - ✅ 支持语音和图像搜索的文件类型过滤
  - ✅ 完善请求参数文档和示例
- **接口文档更新**:
  - 更新 MultimodalRequest 数据模型
  - 添加详细的 multipart/form-data 请求示例
  - 完善前端集成示例代码
  - 更新 MultimodalResponse 模型，支持 search_time 和 ai_models_used 字段

### v1.2.0 (2025-12-08)
- **架构优化**: 删除WebSocket实现，改用HTTP轮询方式
- **新增**: 实时消息轮询API模块 (4个接口)
  - ✅ GET /api/realtime/index/{index_id}/progress - 索引进度查询
  - ✅ GET /api/realtime/search/suggestions - 搜索建议查询
  - ✅ GET /api/realtime/index/active-tasks - 活跃任务查询
  - ✅ GET /api/realtime/polling-config - 轮询配置建议
- **文档更新**: 将WebSocket接口文档更新为轮询方式实现
- **技术优势**: 简化架构、提升兼容性、减少服务器负载

### v1.1.0 (2025-11-25)
- 初始版本发布
- 完整的搜索、索引、配置管理API
- 多模态搜索支持
- WebSocket实时通信
- 前端集成示例
- **新增**: 搜索历史删除API (DELETE /api/search/history) ✅ 已实现
- **新增**: 应用日志查询API (GET /api/system/logs) ✅ 已实现
- **新增**: 日志文件下载API (GET /api/system/logs/download) ✅ 已实现
- **完善**: 补充AppSettings和AppLog数据模型定义

---

## 📋 API实现状态总览

### ✅ 完全实现的API模块
- **搜索API** (6/7): 86% 完成
  - ✅ POST /api/search - 文本搜索
  - ✅ POST /api/search/multimodal - 多模态搜索
  - ✅ GET /api/search/history - 获取搜索历史
  - ✅ DELETE /api/search/history/{id} - 删除单条搜索历史 *(2025-11-25实现)*
  - ✅ DELETE /api/search/history - 清空搜索历史
  - ✅ GET /api/search/suggestions - 搜索建议
  - ⏸️ GET /api/frames/preview/{file_id}_{timestamp} - 视频帧预览图 *(v2.5已暂停)*
  - ✨ 搜索响应扩展：新增数据源字段（source_type、source_url、source_name）*(v3.0规划)*

- **插件化架构** 🚧 规划中 (v3.0)
  - 🚧 插件配置文件管理（data/plugins/*/config.yaml）
  - 🚧 插件自动发现和加载机制
  - 🚧 语雀数据源插件
  - 🔮 更多数据源插件（飞书、Notion等）

- **索引API** (10/10): 100% 完成
- **系统API** (2/2): 100% 完成
  - ✅ GET /api/system/health - 系统健康检查 *(2025-11-28简化)*
  - ✅ GET /api/system/running-status - 系统运行状态 *(2025-12-11新增)*
- **配置API** (6/6): 100% 完成
- **设置管理API** (8/8): 100% 完成 *(2025-12-10新增)*
  - ✅ GET /api/settings/ - 获取所有设置项
  - ✅ GET /api/settings/{key} - 获取指定设置项
  - ✅ GET /api/settings/{key}/value - 获取设置值（原始类型）
  - ✅ POST /api/settings/ - 创建设置项
  - ✅ PUT /api/settings/{key} - 更新设置值
  - ✅ DELETE /api/settings/{key} - 删除设置项
  - ✅ POST /api/settings/batch - 批量创建设置项
  - ✅ POST /api/settings/reset - 重置为默认设置
- **实时消息API** (4/4): 100% 完成 *(2025-12-08新增)*
  - ✅ GET /api/realtime/index/{index_id}/progress - 索引进度查询
  - ✅ GET /api/realtime/search/suggestions - 搜索建议查询
  - ✅ GET /api/realtime/index/active-tasks - 活跃任务查询
  - ✅ GET /api/realtime/polling-config - 轮询配置建议

### 📊 总体统计
- **总接口数**: 37个 *(系统运行状态API 1个，设置管理API 8个，实时消息API 4个)*
- **已实现**: 36个 (97%)
- **已暂停**: 1个 (视频帧预览API - v2.5)
- **规划中**: 插件化架构与语雀数据源 (v3.0)
- **实现完成度**: 97%（基础功能100%，视频画面搜索已暂停，插件化架构规划中）

### 🗑️ 已移除的冗余模块
- **AI模型管理API** (9/9): 已删除 *(功能合并到配置API中)*
  - ❌ GET /api/ai/status - 获取AI模型状态 *(功能重复)*
  - ❌ GET /api/ai/models - 获取可用模型列表 *(功能重复)*
  - ❌ POST /api/ai/load/{model_id} - 加载指定模型 *(功能重复)*
  - ❌ POST /api/ai/unload/{model_id} - 卸载指定模型 *(功能重复)*
  - ❌ POST /api/ai/load-all - 加载所有模型 *(功能重复)*
  - ❌ POST /api/ai/unload-all - 卸载所有模型 *(功能重复)*
  - ❌ GET /api/ai/health - AI模型健康检查 *(功能重复)*
  - ❌ POST /api/ai/benchmark - AI模型性能基准测试 *(功能重复)*
  - ❌ GET /api/ai/text-embedding - 文本嵌入测试 *(功能重复)*

**最后更新时间**: 2026年2月22日
**更新内容**: 插件化架构与语雀数据源 - 新增第5节插件化架构配置，搜索响应扩展数据源字段，更新章节编号

---

**维护团队**: 小遥搜索开发组
**技术支持**: [GitHub Issues](https://github.com/your-repo/issues)
**文档更新**: 2026-02-22