# 开发任务清单：飞书文档数据源

> **文档类型**：开发任务清单
> **特性状态**：已完成
> **创建时间**：2026-03-31
> **最后更新**：2026-03-31 (v1.0 - 初始版本)
> **预计总工期**：3-5天

---

## 设计原则：零配置 + 元数据解析 + 复用现有架构

本特性基于**插件化架构**（由 plugins+yuque 特性建立），飞书数据源采用**零配置元数据解析**方案：
- **复用现有插件基础设施**：BasePlugin、DataSourcePlugin、PluginLoader
- **无需配置文件**：自动识别飞书导出格式
- **无需外部同步**：用户手动导出，无 API 调用
- **性能优化**：仅解析文件末尾500字符
- **完全兼容**：前端、数据库、索引服务均已支持

---

## 任务清单总览

| 阶段 | 任务数 | 预计时间 | 优先级 |
|------|--------|----------|--------|
| 一、飞书插件实现 | 3 | 1-2天 | P0 |
| 二、插件部署与验证 | 2 | 0.5-1天 | P0 |
| 三、测试 | 3 | 1-2天 | P0 |
| 四、文档 | 1 | 0.5天 | P1 |
| **总计** | **9** | **3-5天** | - |

**重要说明**：
- ✅ 插件基础设施（BasePlugin、DataSourcePlugin、PluginLoader）由 plugins+yuque 特性完成
- ✅ 前端UI支持（飞书数据源标识）由 plugins+yuque 特性完成
- ✅ 数据库字段（source_type、source_url）由 plugins+yuque 特性完成
- ✅ 索引服务集成（调用插件获取元数据）由 plugins+yuque 特性完成
- 🔧 本特性仅需实现飞书插件本身的代码

---

## 一、飞书插件实现（1-2天）

### 1.1 飞书数据源插件核心实现

| 任务 | 预计时间 | 优先级 | 状态 | 依赖 |
|------|----------|--------|------|------|
| 创建 FeishuDataSource 类 | 1小时 | P0 | ⬜ | 无 |
| 实现 get_metadata 方法 | 0.5小时 | P0 | ⬜ | 无 |
| 实现 initialize 方法 | 0.5小时 | P0 | ⬜ | 无 |
| 实现 sync 方法（空实现） | 0.5小时 | P0 | ⬜ | 无 |
| 实现 cleanup 方法 | 0.5小时 | P0 | ⬜ | 无 |
| **小计** | **3小时** | | | |

**验收标准**：
- [ ] FeishuDataSource 继承自 DataSourcePlugin
- [ ] get_metadata 返回正确的插件元数据
- [ ] 所有必需方法正确实现

**文件位置**: `data/plugins/datasource/feishu/plugin.py`

### 1.2 元数据解析实现

| 任务 | 预计时间 | 优先级 | 状态 | 依赖 |
|------|----------|--------|------|------|
| 实现 get_file_source_info 方法 | 2小时 | P0 | ⬜ | 1.1 |
| 定义飞书元数据正则表达式模式 | 1小时 | P0 | ⬜ | 无 |
| 实现标准格式解析（中文） | 1小时 | P0 | ⬜ | 无 |
| 实现兼容格式解析（英文） | 1小时 | P1 | ⬜ | 无 |
| 添加解析性能优化（仅读取末尾500字符） | 0.5小时 | P0 | ⬜ | 无 |
| 添加解析错误处理与日志 | 1小时 | P0 | ⬜ | 无 |
| **小计** | **6.5小时** | | | |

**飞书元数据格式**：
```markdown
---
> 更新: 2026-03-30 02:52:46
> 来源类型: feishu
> 原文: <https://feishu.cn/wiki/MZKMwqpljiod1ak38Cscnr8hnkh>
---
```

**核心逻辑**：
```python
def get_file_source_info(self, file_path: str, content: str) -> Dict[str, Any]:
    """
    获取文件的数据源信息

    1. 读取文件末尾 500 字符
    2. 使用正则表达式匹配飞书元数据
    3. 提取 source_type 和 source_url
    4. 返回结果或 None（非飞书文档）
    """
    # 仅读取末尾优化性能
    content_tail = content[-500:] if len(content) > 500 else content

    # 遍历匹配模式
    for pattern in self.FEISHU_METADATA_PATTERNS:
        match = re.search(pattern, content_tail, re.IGNORECASE | re.DOTALL)
        if match:
            return {
                "source_type": "feishu",
                "source_url": match.group(1).strip()
            }

    return {"source_type": None, "source_url": None}
```

**正则表达式模式**：
```python
# 标准格式（中文）
FEISHU_METADATA_PATTERNS = [
    r'>\s*来源类型:\s*feishu\s*>?\s*原文:\s*<(https://[^\s>]+)>',
    # 兼容格式（英文）
    r'>\s*Source:\s*feishu\s*>?\s*URL:\s*<(https://[^\s>]+)>',
]
```

**验收标准**：
- [ ] 能够正确解析标准飞书元数据格式
- [ ] 能够提取原文链接
- [ ] 解析失败时返回 None，不影响文件索引
- [ ] 解析性能 < 1ms/文件
- [ ] 错误被正确捕获和记录

**文件位置**: `data/plugins/datasource/feishu/plugin.py`

### 1.3 插件目录结构与文档

| 任务 | 预计时间 | 优先级 | 状态 | 依赖 |
|------|----------|--------|------|------|
| 创建插件目录结构 | 0.5小时 | P0 | ⬜ | 无 |
| 创建 README.md 使用说明 | 1.5小时 | P1 | ⬜ | 1.1, 1.2 |
| **小计** | **2小时** | | | |

**插件目录结构**：
```
data/plugins/
└── datasource/
    └── feishu/
        ├── plugin.py          # 插件实现
        └── README.md          # 使用说明
```

**README.md 内容**：
```markdown
# 飞书文档插件

识别从飞书导出的 Markdown 文档并提取元数据。

## 使用方法

1. 从飞书导出文档为 Markdown 格式
2. 将导出的文件放置到扫描目录
3. 构建索引
4. 搜索结果将显示"飞书"来源标识

## 支持的格式

飞书导出的 Markdown 文档末尾包含以下元数据：

```markdown
---
> 更新: 2026-03-30 02:52:46
> 来源类型: feishu
> 原文: <https://feishu.cn/wiki/MZKMwqpljiod1ak38Cscnr8hnkh>
---
```

## 常见问题

**Q: 解析失败怎么办？**
A: 解析失败不影响文件索引，只是不会显示"飞书"标识。检查导出格式是否完整。

**Q: 原文链接会过期吗？**
A: 如果您有飞书文档的访问权限，原文链接长期有效。
```

**验收标准**：
- [ ] 插件目录结构正确
- [ ] README.md 内容完整
- [ ] 无需 config.yaml（零配置）

**文件位置**: `data/plugins/datasource/feishu/`

---

## 二、插件部署与验证（0.5-1天）

### 2.1 本地部署测试

| 任务 | 预计时间 | 优先级 | 状态 | 依赖 |
|------|----------|--------|------|------|
| 创建插件目录 | 0.25小时 | P0 | ⬜ | 一 |
| 复制插件文件 | 0.25小时 | P0 | ⬜ | 一 |
| 重启后端服务 | 0.25小时 | P0 | ⬜ | 一 |
| 验证插件加载日志 | 0.5小时 | P0 | ⬜ | 2.1.3 |
| **小计** | **1.25小时** | | | |

**验证步骤**：
1. 创建目录 `data/plugins/datasource/feishu/`
2. 放置 `plugin.py` 和 `README.md`
3. 重启后端服务
4. 查看启动日志确认插件加载

**预期日志**：
```
✅ 插件系统启动完成，已加载 2 个插件
  - yuque: 语雀知识库
  - feishu: 飞书文档
```

**验收标准**：
- [ ] 插件目录创建成功
- [ ] 后端服务正常启动
- [ ] 日志显示飞书插件已加载

### 2.2 集成验证

| 任务 | 预计时间 | 优先级 | 状态 | 依赖 |
|------|----------|--------|------|------|
| 准备飞书导出测试文件 | 0.5小时 | P0 | ⬜ | 无 |
| 构建索引测试 | 0.5小时 | P0 | ⬜ | 2.1 |
| 搜索测试验证数据源标识 | 1小时 | P0 | ⬜ | 2.2.2 |
| 验证原文链接跳转 | 0.5小时 | P0 | ⬜ | 2.2.3 |
| **小计** | **2.5小时** | | | |

**测试文件准备**：
```markdown
# 飞书测试文档

这是测试内容。

---
> 更新: 2026-03-30 02:52:46
> 来源类型: feishu
> 原文: <https://feishu.cn/wiki/test123>
---
```

**验证步骤**：
1. 将测试文件放到扫描目录
2. 调用 `/api/index/create` 构建索引
3. 调用 `/api/search` 搜索关键词
4. 验证搜索结果包含：
   - `source_type: "feishu"`
   - `source_url: "https://feishu.cn/wiki/test123"`
   - 前端显示"飞书"标识

**验收标准**：
- [ ] 飞书文档被正确索引
- [ ] 搜索结果显示飞书标识
- [ ] 原文链接可跳转
- [ ] 非飞书文档不受影响

---

## 三、测试（1-2天）

### 3.1 单元测试

| 任务 | 预计时间 | 优先级 | 状态 | 依赖 |
|------|----------|--------|------|------|
| get_metadata 方法测试 | 0.5小时 | P0 | ⬜ | 1.1 |
| 标准格式解析测试 | 1小时 | P0 | ⬜ | 1.2 |
| 兼容格式解析测试 | 0.5小时 | P1 | ⬜ | 1.2 |
| 非飞书文件测试 | 0.5小时 | P0 | ⬜ | 1.2 |
| 长文件解析性能测试 | 1小时 | P1 | ⬜ | 1.2 |
| 异常处理测试 | 0.5小时 | P0 | ⬜ | 1.2 |
| **小计** | **4小时** | | | |

**测试用例**：

| 用例 | 输入 | 预期输出 |
|-----|------|---------|
| 标准格式 | 包含完整飞书元数据的文件 | source_type=feishu, source_url 正确提取 |
| 兼容格式 | 英文元数据格式 | source_type=feishu |
| 非飞书文件 | 普通 Markdown 文件 | source_type=None |
| 空文件 | 空内容文件 | source_type=None |
| 超长文件 | 10000+行内容末尾含元数据 | 正确解析，性能 < 1ms |

**单元测试文件位置**：`tests/plugins/test_feishu_plugin.py`

**覆盖率目标**：≥ 80%

**验收标准**：
- [ ] 所有测试用例通过
- [ ] 代码覆盖率 ≥ 80%
- [ ] 无 P0/P1 缺陷

### 3.2 集成测试

| 任务 | 预计时间 | 优先级 | 状态 | 依赖 |
|------|----------|--------|------|------|
| 端到端飞书文档搜索测试 | 2小时 | P0 | ⬜ | 二 |
| 混合数据源搜索测试 | 2小时 | P0 | ⬜ | 二 |
| 插件错误隔离测试 | 1小时 | P0 | ⬜ | 1.2 |
| **小计** | **5小时** | | | |

**集成测试用例**：

| 用例 | 说明 | 预期结果 |
|-----|------|---------|
| E2E 飞书搜索 | 从导出到搜索的完整流程 | 搜索结果正确显示飞书标识 |
| 混合数据源 | 本地+语雀+飞书文档一起搜索 | 结果包含三种数据源标识 |
| 错误隔离 | 飞书解析失败不影响其他文件 | 本地文件正常索引 |

**集成测试文件位置**：`tests/integration/test_feishu_integration.py`

**验收标准**：
- [ ] 所有集成测试通过
- [ ] 无回归缺陷
- [ ] 错误隔离正确

### 3.3 性能测试

| 任务 | 预计时间 | 优先级 | 状态 | 依赖 |
|------|----------|--------|------|------|
| 元数据解析性能测试 | 1小时 | P1 | ⬜ | 1.2 |
| 扫描性能影响测试 | 1小时 | P1 | ⬜ | 二 |
| **小计** | **2小时** | | | |

**性能指标**：

| 指标 | 目标值 | 测量方法 |
|-----|-------|---------|
| 单文件解析时间 | < 1ms | 单元测试计时 |
| 扫描性能影响 | < 5% | 对比有/无插件扫描时间 |

**验收标准**：
- [ ] 单文件解析 < 1ms
- [ ] 扫描性能影响 < 5%

---

## 四、文档（0.5天）

### 4.1 使用文档

| 任务 | 预计时间 | 优先级 | 状态 | 依赖 |
|------|----------|--------|------|------|
| 编写 README.md | 1.5小时 | P1 | ⬜ | 一 |
| 更新全局文档（CLAUDE.md） | 0.5小时 | P1 | ⬜ | 三 |
| **小计** | **2小时** | | | |

**README.md 内容**：
- 插件介绍
- 使用方法
- 支持的格式
- 常见问题
- 故障排除

**全局文档更新**：
- 在 `docs/CLAUDE.md` 中添加飞书插件说明
- 更新插件列表

**验收标准**：
- [ ] README.md 内容完整
- [ ] 全局文档已更新

---

## 任务依赖关系图

```
┌─────────────────────────────────────────────────────────────┐
│                   飞书插件实现(一)                             │
│  创建类(1.1) → 元数据解析(1.2) → 目录结构(1.3)                │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                   插件部署与验证(二)                          │
│  本地部署(2.1) → 集成验证(2.2)                                 │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                   测试(三)                                    │
│  单元测试(3.1) → 集成测试(3.2) → 性能测试(3.3)               │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                   文档(四)                                    │
│  README.md + 全局文档更新                                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 里程碑

| 里程碑 | 预计完成时间 | 关键交付物 |
|--------|-------------|-----------|
| M1: 飞书插件完成 | 第1天 | plugin.py 实现，元数据解析功能 |
| M2: 部署验证完成 | 第2天 | 插件加载成功，搜索结果显示飞书标识 |
| M3: 测试完成 | 第3-4天 | 单元测试、集成测试通过，性能达标 |
| M4: 文档完成 | 第4-5天 | README.md 和全局文档更新 |

---

## 风险与应对

| 风险项 | 影响 | 应对措施 | 负责人 |
|-------------|----------|--------|
| 飞书导出格式变化 | 元数据解析失败 | 多种格式兼容，提供清晰的格式说明 | 开发 |
| 性能影响 | 扫描变慢 | 仅解析末尾500字符，性能测试验证 | 开发 |
| 正则表达式错误 | 解析失败 | 多种模式匹配，充分测试 | 开发 |

---

## 完成标准

- [ ] 所有P0任务完成
- [ ] 单元测试覆盖率 ≥ 80%
- [ ] 集成测试全部通过
- [ ] 性能指标达标（解析 < 1ms，扫描影响 < 5%）
- [ ] 飞书文档可被正确识别
- [ ] 搜索结果显示飞书标识
- [ ] 原文链接可跳转
- [ ] README.md 文档完整

---

## 关键文件清单

| 文件路径 | 说明 | 类型 |
|---------|------|------|
| `data/plugins/datasource/feishu/plugin.py` | 飞书插件实现 | 新建 |
| `data/plugins/datasource/feishu/README.md` | 使用说明 | 新建 |
| `tests/plugins/test_feishu_plugin.py` | 单元测试 | 新建 |
| `tests/integration/test_feishu_integration.py` | 集成测试 | 新建 |
| `docs/CLAUDE.md` | 全局文档更新 | 修改 |

**无需修改的文件**（已有支持）：
- ✅ `backend/app/plugins/base.py` - 通用插件基类（已有）
- ✅ `backend/app/plugins/interface/datasource.py` - 数据源接口（已有）
- ✅ `backend/app/plugins/loader.py` - 插件加载器（已有）
- ✅ `backend/app/services/file_index_service.py` - 索引服务（已有）
- ✅ `frontend/src/renderer/src/components/SearchResultCard.vue` - 搜索结果组件（已有）
- ✅ `backend/app/models/file.py` - 数据库模型（已有）

---

## 版本变更历史

### v1.0.0 (2026-03-31) - 初始版本

**设计原则**：
- 零配置：无需 config.yaml
- 元数据解析：实现 get_file_source_info() 方法
- 复用现有架构：基于 plugins+yuque 特性

**简化内容**：
- 🔄 无需外部同步（用户手动导出）
- 🔄 无需配置文件
- 🔄 无需修改前端、数据库、索引服务

**工作量说明**：
- 任务数：9
- 预计工期：3-5天
- 主要工作量：飞书插件本身实现 + 测试

---

**文档版本**: v1.0
**创建时间**: 2026-03-31
**最后更新**: 2026-03-31 (v1.0 - 初始版本)
**预计总工期**: 3-5个工作日
**文档状态**: 待批准
